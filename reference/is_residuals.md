# Test if a table contains residuals.

This function returns `TRUE` for objects that contains the column name
'.residuals'.

## Usage

``` r
is_residuals(object)
```

## Arguments

- object:

  An object to detect if it provides from
  modeltime::modeltime_residuals().
