# Succinct summary of Modeltime Tables

`type_sum` controls how objects are shown when inside tibble columns.

## Usage

``` r
# S3 method for class 'mdl_time_tbl'
type_sum(x)
```

## Arguments

- x:

  A `mdl_time_tbl` object to summarise.

## Value

A character value.
