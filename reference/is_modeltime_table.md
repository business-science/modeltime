# Test if object is a Modeltime Table

This function returns `TRUE` for objects that contain class
`mdl_time_tbl`

## Usage

``` r
is_modeltime_table(object)
```

## Arguments

- object:

  An object to detect if is a Modeltime Table
